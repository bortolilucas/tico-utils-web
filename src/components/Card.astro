---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div {...rest} class:list={["bg-white p-8 rounded-2xl shadow-sm", className]}>
  <slot />
</div>
